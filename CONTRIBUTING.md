# 参与贡献

## 开始之前

先阅读 [README](README.md) 和 [算法说明](docs/ALGORITHMS.md)。如需改变识别口径，先开 Issue 说明输入、失败案例、预期输出及评价方式，避免将检测优化与 UI 修改混在一起。

## 提交 PR

1. 从最新 `main` 创建分支，每个 PR 尽量解决一个问题。
2. 写清动机、变更范围、兼容性及测试结果。
3. 运行 `node scripts/test-public.cjs`；Python 变更按 README 设置 PYTHONPATH 后运行视觉测试。
4. 区分单元测试、模型实测和实机验收；缺少模型/SDK 时明确列出未验证内容。
5. 保留第三方声明，禁止提交密钥、设备账户数据、私人视频和未授权资产。

## 算法变更规则

- `vision-v32/` 是冻结运行时，不直接调整其阈值或覆盖文件。需要新算法时建立新的版本，保留对照基线。
- 进食行为证据、统计口径、贴脸排名是不同层级，改动时分别评估。
- 报告漏检/误检、片段覆盖、时间边界误差及耗时；不要只展示一个成功视频。
- 附上输入分辨率、帧率、录像时长、方向、ROI、模型版本、机器配置和各阶段耗时。
- 测试素材必须取得授权并去除个人信息；不要求用户公开家庭录像。

## 报告问题

提供提交号、操作步骤、期望/实际结果和脱敏错误日志。摄像头问题注明设备/SDK版本与网络环境；不要附带令牌或带签名的回放 URL。

安全漏洞请按 [SECURITY.md](SECURITY.md) 私下联系仓库维护者。
